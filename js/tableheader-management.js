$(function () {
    var storedheader
    var headerHtml
    var editableheader
    var newheaderText
    /*
    $(document).on("dblclick", ".releasename", function () {
        $("span").text("header clicked");
        storedheader = $(this);
        headerHtml = $(this).html();
        editableheader = $("<textarea />");
        editableheader.val(headerHtml);
        $(this).replaceWith(editableheader);
        editableheader.focus();
        // setup the blur event for this new textarea
        editableheader.blur(editableheaderBlurred);
    });

    function editableheaderBlurred() {
        newheaderText = $(this).val();
        storedheader.text(newheaderText);
        $(this).replaceWith(storedheader);

    }
    */
});